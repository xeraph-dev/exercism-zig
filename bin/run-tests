#!/usr/bin/env bash

GREEN="\e[32m"
YELLOW="\e[33m"
RESET="\e[0m"

jq -rc '.exercises.practice[].slug' config.json | while read slug; do
    echo -e "${YELLOW}Running tests for $GREEN$slug$RESET"
    zig test "exercises/practice/$slug/test_${slug//-/_}.zig" || break
done